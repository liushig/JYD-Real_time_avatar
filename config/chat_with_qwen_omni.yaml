# CUDA_VISIBLE_DEVICES=2 uv run src/demo.py --config config/chat_with_qwen_omni.yaml

# CUDA_VISIBLE_DEVICES=2 nohup uv run src/demo.py --config config/chat_with_qwen_omni.yaml > logs/8282.log 2>&1 &

# 提升稳定性

default:
  logger:
    log_level: "INFO"
  service:
    host: "0.0.0.0"
    port: 8289
    cert_file: "ssl_certs/localhost.crt"
    cert_key: "ssl_certs/localhost.key"
  chat_engine:
    model_root: "models"
    concurrent_limit: 1
    handler_search_path:
      - "src/handlers"
    handler_configs:
      RtcClient:
        module: client/rtc_client/client_handler_rtc
        # 如果你希望 RTC 客户端连接时间长一些（适合长期对话），保留 36000 秒（约 10 小时）
        # connection_ttl: 36000
        # max time a session will last for
        connection_ttl: 900
      SileroVad:
        module: vad/silerovad/vad_handler_silero
        speaking_threshold: 0.5
        start_delay: 2048
        end_delay: 5000
        buffer_look_back: 5000
        speech_padding: 512
      SenseVoice:
        enabled: True
        module: asr/sensevoice/asr_handler_sensevoice
        model_name: "iic/SenseVoiceSmall"
        
      QwenOmni:
        enabled: true
        module: llm/qwen_omni/llm_handler_qwen_omni
        model_name: "qwen-omni-turbo-realtime"
        # model_name: "qwen3-omni-flash"
        # api_key: "sk-64ff5b9a78f943c7b304244d911dc1d0" # default=os.getenv("DASHSCOPE_API_KEY") api_key可以通过此处设置，或者通过环境变量，没设必挂
        # api_key: "sk-10405643d30443d8b641e0771ed56d2f"   # 含韵免费APIkey,2026年2月25到期
        # api_key: "sk-5f2d36a950e1406aa9a15b9bcb8b4616"
        api_key: "sk-a588f70e9a614e4f87533ff792d2e358"  # 天琪
        # voice: "Ethan" 
        voice: "Cherry" #"Chelsie", "Serena", "Ethan", "Cherry"
        enable_video_input: true
        video_frame_interval_ms: 1000 # 每1000ms发送一帧图片，防止拥塞，可按需调整，根据omni的文档，不要低于500ms
        enable_text_output: true
        persona_prompt: "你不是通义千问，你不是阿里云，你是一名由竞业达开发的数字老师。用简短的两三句对话来回答用户的问题，并在对话内容中加入合适的标点符号，不需要讨论标点符号相关的内容。"
        input_audio_format: "PCM_16000HZ_MONO_16BIT"
        output_audio_format: "PCM_24000HZ_MONO_16BIT"
        enable_turn_detection: false # 目前暂未支持服务端vad模式，此处默认为false，不要修改
        enable_input_transcription: false # 手动模式下，omni的输入的语音识别效果非常差，几乎不可用，默认关闭，直接使用正常asr的结果
        transcription_model: "gummy-realtime-v1"
        



      # Musetalk和LiteAvatar可以通过配置自由选，默认使用LiteAvatar了

      AvatarMusetalk:
        module: avatar/musetalk/avatar_handler_musetalk
        fps: 20 # Video frame rate
        batch_size: 2  # Batch processing frame count, must be greater than 2
        avatar_video_path: "src/handlers/avatar/musetalk/MuseTalk/data/video/1224_1747.mp4"  # Initialization video path
        avatar_model_dir: "models/musetalk/avatar_model"  # Default avatar model directory
        force_create_avatar: false  # Whether to force regenerate digital human data
        debug: false  # Whether to enable debug mode
        multi_thread_inference: false  # true: use 2 threads to inference (unet, vae), false: use single-thread to inference


      # LiteAvatar:
      #   module: avatar/liteavatar/avatar_handler_liteavatar
      #   # 默认
      #   # avatar_name: 20250408/sample_data
      #   # 女老师
      #   # avatar_name: 20250612/P17L8MoMPLjyWLWn7-jCrnAw
      #   # 男帅哥
      #   # avatar_name: 20250408/P1tnueW7ufnZuEVix6rmAOzw
      #   # 男老师
      #   # avatar_name: 20250612/P1GHtv7jDegag9hgcPFAW7Ng
      #   # 男青年
      #   # avatar_name: 20250612/P1PNFMLjxwu9Cbi2nyK5yoaA
      #   # 灰色背景女老师
      #   avatar_name: 20250612/P18qnW9QV47heC5q0hNWUiqQ
      #   fps: 25
      #   debug: false
      #   enable_fast_mode: false
      #   use_gpu: true


